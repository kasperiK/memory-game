import React from 'react';
import { StyledHeader } from '../../StyledComponents';

const GameHeader = () => (
	<StyledHeader>
		<header>
			<h1>Muistipeli</h1>
		</header>
	</StyledHeader>
);

export default GameHeader;